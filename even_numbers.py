scores = []
def even_numbers_in_numbers(pass_in_something):
    even_numbers = []
    for scores in pass_in_something:
        if scores % 2 == 0:
            even_numbers.append(scores)
    return even_numbers

for numbers in range(50):
    scores.append(numbers)
print(even_numbers_in_numbers(scores))